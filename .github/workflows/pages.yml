name: Deploy to GitHub Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build site
        run: |
          # Create output directory
          mkdir -p _site

          # Copy all HTML files
          cp entry.html _site/
          cp schedule.html _site/
          cp myplan.html _site/
          cp friends.html _site/

          # Copy entry.html as index.html for root URL
          cp entry.html _site/index.html

          # Copy CSS and JS
          cp styles.css _site/
          cp nav.css _site/
          cp app.js _site/

          # Copy data files
          cp xml.xml _site/
          cp README.md _site/
          cp firebase-rules.json _site/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
